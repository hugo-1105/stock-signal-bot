# ==========================================
# Dockerfile — Render Background Worker
# US Multi-Stock Signal Bot
# ==========================================

# Use a lightweight Python image
FROM python:3.11-slim

# Set working directory
WORKDIR /app

# Copy dependencies first
COPY requirements.txt .

# Install Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the bot code
COPY . .

# Set timezone to UK
ENV TZ=Europe/London

# Prevent output buffering (immediate log flush)
ENV PYTHONUNBUFFERED=1

# Run the bot script
CMD ["python", "Stock_Auto.py"]

# Ensure Python prints instantly (no buffering)
ENV PYTHONUNBUFFERED=1

# Command to run the bot continuously
CMD ["python", "Stock_Auto.py"]
